<%- include("../../partials/header"); %>
    <body>
        <%- include("../../partials/navbar.ejs"); %>
            <div class="container">
                <div class="card bg-light m-2">
                    <div class="card-header dark-bg p-4">
                        <h2 class="p-2 mt-2 text-white">Edição de categoria</h2>
                    </div>
                    <div class="card-body p-4">
                        <form method="POST" action="/categories/update">
                            <label>Título da categoria:</label>
                            <input type="text" name="title" placeholder="Defina o título da categoria" value="<%= category.title %>"
                                class="form-control">
                            <input type="hidden" name="id" value="<%= category.id %>">
                            <button class="btn btn-secondary mt-4">Atualizar</button>
                        </form>
                    </div>
                </div>
            </div>
    </body>
<%- include("../../partials/footer.ejs"); %>